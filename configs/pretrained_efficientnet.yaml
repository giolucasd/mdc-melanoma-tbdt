model:
  name: efficientnet_b0
  pretrained: true

training:
  learning_rate: 0.001
  weight_decay: 0.0001
  warmup_epochs: 5
  max_epochs: 50
  grad_accum: 2

data:
  batch_size: 256
  num_workers: 4
